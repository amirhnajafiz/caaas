apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "caaas.fullname" . }}-test-connection"
  labels:
    {{- include "caaas.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "caaas.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
