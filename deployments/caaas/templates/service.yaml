apiVersion: v1
kind: Service
metadata:
  name: {{ include "caaas.fullname" . }}
  labels:
    {{- include "caaas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caaas.selectorLabels" . | nindent 4 }}
